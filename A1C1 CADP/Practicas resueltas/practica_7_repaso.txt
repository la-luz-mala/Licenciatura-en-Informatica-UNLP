// 1. Una productora nacional realiza un casting de personas para la selección de actores extras de una nueva
// película, para ello se debe leer y almacenar la información de las personas que desean participar de dicho
// casting. De cada persona se lee: DNI, apellido y nombre, edad y el código de género de actuación que
// prefiere (1: drama, 2: romántico, 3: acción, 4: suspenso, 5: terror). La lectura finaliza cuando llega una
// persona con DNI 33555444, la cual debe procesarse.
// Una vez finalizada la lectura de todas las personas, se pide:
// a. Informar la cantidad de personas cuyo DNI contiene más dígitos pares que impares.
// b. Informar los dos códigos de género más elegidos.
// c. Realizar un módulo que reciba un DNI, lo busque y lo elimine de la estructura. El DNI puede no existir.
// Invocar dicho módulo en el programa principal.

tengo que
    repetir
        leer persona
        almacenar persona
    hasta que DNI = 33555444

recorrer la lista de nuevo y por cada uno:
    desarmar(dni) // son 8 dígitos así que
        for i:=1 to 8 do
            if ((dni mod 2) = 1) then
                impar:= impar+1;
            else
                par := par +1;
        // afuera del for
        if (par > impar) then (maspar:=maspar+1) else (masimpar:=masimpar+1)
    case genero of
        1:gen1:=gen1+1;
        2:gen2:=gen2+1;
        3:gen3:=gen3+1;
        4:gen4:=gen4+1;
        5:gen5:=gen5+1;
    if (gen1 > gen2) and (gen1 > gen3) and (gen1 > gen4) and (gen1 > gen5) then writeln('gen1 es mayor');
    else if (gen2 > gen3) and (gen2 > gen4) and (gen2 > gen5) then writeln('gen2 es mayor');
        else if (gen3 > gen4) and (gen3 > gen5) then writeln('gen3 es mayor');
            else if (gen4 > gen5) then writeln('gen4 es mayor');
                else writeln('gen5 es mayor');
    
    // afuera de esto
    eliminar(33333333)

// y fuera del programa
    function eliminar
        while (no se me termina la lista) and (dato != dni)
            avanzo un nodo
        //afuera del while
        if nodoactual.dato = dnibuscado and lista != nil
            enlazo nodo previo con sig
            eliminar:= true
        else
            eliminar:=false;

    
Program uno;
Type
    persona = record
        dni:longint;
        nombreComp:string;
        edad:integer;
        genero:integer;
    end;

    lista = ^nodo;

    nodo = record
        dato: persona; // el nodo es 1 campo de dato + 1 campo de dirección
        sig: lista;
    end;
    
Var
    actores:lista;
    pri:integer;
Procedure iniciar(list)
    begin
        new(list)
    end;
Procedure agregarAdelante (var actores:lista; p:persona) // actores por ref, persona por valor
    var
        aux:lista;
    begin
        new(aux); // reservo el espacio para el nodo
        aux^.dato := persona; // cargo el dato
        aux^.sig := actores; // realizo el enlace, si la lista está vacía debería ser NIL
        actores := aux;
    end;

Begin
    pri:=nil;
    iniciar(actores);
    agregarAdelante(actores,pri)
End.
type


primero leo
writeln ingrese dni
repeat
    
until (dni = 33555444);